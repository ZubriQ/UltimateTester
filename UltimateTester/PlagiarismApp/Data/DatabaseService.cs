namespace PlagiarismApp.Data
{
    public class DatabaseService
    {
        //private PlagiarismContext _context { get; set; }

        //public DatabaseService()
        //{
        //    _context = new PlagiarismContext();
        //}

        //// TODO: perhaps return bool or a user ?
        //public async Task SignIn(string? login, string? password)
        //{
        //    await Task.Run(() =>
        //    {
        //        var user = _context.Users.Where(u => u.Login == login).FirstOrDefault();
        //        if (user != null)
        //        {
        //            // TODO: some hashing algorithm ?
        //            if (user?.Password == password)
        //            {
        //                // success...
        //            }
        //        }
        //    });
        //}
    }
}
